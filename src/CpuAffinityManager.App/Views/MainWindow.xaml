<Window x:Class="CpuAffinityManager.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource Loc.AppTitle}" Icon="/Assets/app.ico"
        Height="820" Width="1200" MinHeight="560" MinWidth="860"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border Background="{StaticResource BgBrush}" CornerRadius="12">
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Margin="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="38"/> <RowDefinition Height="*"/> <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" Background="{StaticResource SurfaceBrush}" MouseDown="TitleBar_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/> <ColumnDefinition Width="*"/> <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Padding="16,0" Background="{StaticResource SidebarBgBrush}" CornerRadius="10,0,0,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="/Assets/app-icon.png" Width="22" Height="22" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{DynamicResource Loc.AppTitle}" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextOnDarkBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Grid Grid.Column="1" Background="Transparent"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="─" Style="{StaticResource GhostButton}" Click="Minimize_Click" Width="40" Height="32" FontSize="14"/>
                <Button Content="□" Style="{StaticResource GhostButton}" Click="Maximize_Click" Width="40" Height="32" FontSize="14"/>
                <Button Content="✕" Style="{StaticResource GhostButton}" Click="Close_Click" Width="40" Height="32" FontSize="14"/>
            </StackPanel>
        </Grid>

        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/> <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="{StaticResource SidebarBgBrush}" CornerRadius="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Margin="0,8,0,0">
                        <ListBox x:Name="NavList" SelectionChanged="NavList_SelectionChanged" Background="Transparent" BorderThickness="0">
                            <ListBoxItem Style="{StaticResource NavItem}" Tag="dashboard">
                                <StackPanel Orientation="Horizontal"><TextBlock Text="📊" Margin="0,0,10,0"/><TextBlock Text="{DynamicResource Loc.Dashboard}"/></StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource NavItem}" Tag="processes">
                                <StackPanel Orientation="Horizontal"><TextBlock Text="📋" Margin="0,0,10,0"/><TextBlock Text="{DynamicResource Loc.Processes}"/></StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource NavItem}" Tag="rules">
                                <StackPanel Orientation="Horizontal"><TextBlock Text="📏" Margin="0,0,10,0"/><TextBlock Text="{DynamicResource Loc.Rules}"/></StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource NavItem}" Tag="settings">
                                <StackPanel Orientation="Horizontal"><TextBlock Text="⚙️" Margin="0,0,10,0"/><TextBlock Text="{DynamicResource Loc.Settings}"/></StackPanel>
                            </ListBoxItem>
                        </ListBox>
                    </StackPanel>
                    <Border Grid.Row="1" VerticalAlignment="Bottom" Padding="16,12" Margin="8,0,8,12" CornerRadius="8" Background="{StaticResource SidebarHoverBrush}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Loc.CpuTopology}" Foreground="{StaticResource TextMutedBrush}" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TextBlock x:Name="SidebarCpuInfo" Foreground="{StaticResource TextOnDarkBrush}" FontSize="11"><Run Text="{DynamicResource Loc.Detecting}"/></TextBlock>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Content -->
            <Border Grid.Column="1" Background="{StaticResource BgBrush}" CornerRadius="0,0,10,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock x:Name="PageTitle" Text="{DynamicResource Loc.Dashboard}" Style="{StaticResource Heading1}" Margin="0,0,0,2"/>
                            <TextBlock x:Name="PageSubtitle" Text="{DynamicResource Loc.AppSubtitle}" Style="{StaticResource BodyText}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button x:Name="BtnScanNow" Content="{DynamicResource Loc.ScanNow}" Style="{StaticResource PrimaryButton}" Click="ScanNow_Click" Margin="0,0,8,0"/>
                            <Button Content="{DynamicResource Loc.AddRule}" Style="{StaticResource PrimaryButton}" Click="AddRule_Click"/>
                        </StackPanel>
                    </Grid>

                    <!-- ═══ Dashboard ═══ -->
                    <ScrollViewer Grid.Row="1" x:Name="DashboardPage" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,6,0" Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource Loc.ProcessesRunning}" Style="{StaticResource CaptionText}"/>
                                        <TextBlock x:Name="StatProcessCount" Text="--" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{DynamicResource Loc.Running}" Style="{StaticResource CaptionText}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource Card}" Margin="3,0" Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource Loc.RulesActive}" Style="{StaticResource CaptionText}"/>
                                        <TextBlock x:Name="StatRulesCount" Text="--" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                                        <TextBlock Text="{DynamicResource Loc.Configured}" Style="{StaticResource CaptionText}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource Card}" Margin="3,0" Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource Loc.PCores}" Style="{StaticResource CaptionText}"/>
                                        <TextBlock x:Name="StatPcoreCount" Text="--" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PcoreBrush}"/>
                                        <TextBlock Text="{DynamicResource Loc.Performance}" Style="{StaticResource CaptionText}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="3" Style="{StaticResource Card}" Margin="6,0,0,0" Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource Loc.ECores}" Style="{StaticResource CaptionText}"/>
                                        <TextBlock x:Name="StatEcoreCount" Text="--" FontSize="28" FontWeight="Bold" Foreground="{StaticResource EcoreBrush}"/>
                                        <TextBlock Text="{DynamicResource Loc.Efficiency}" Style="{StaticResource CaptionText}"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="1.4*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource Card}" Padding="20" Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource Loc.CpuTopology}" Style="{StaticResource Heading2}" Margin="0,0,0,8"/>
                                        <ItemsControl x:Name="CoreVisualList">
                                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Width="48" Height="48" Background="{Binding ColorBrush}" CornerRadius="8" Margin="3" ToolTip="{Binding Tooltip}">
                                                        <TextBlock Text="{Binding Index}" Foreground="White" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource Card}" Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource Loc.RuleSummary}" Style="{StaticResource Heading2}" Margin="0,0,0,8"/>
                                        <ItemsControl x:Name="RuleSummaryList">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Padding="10,8" Margin="0,2" CornerRadius="6" Background="{StaticResource SurfaceHoverBrush}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Border Width="8" Height="8" Background="{Binding LevelColor}" CornerRadius="4" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding DisplayText}" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- ═══ Processes ═══ -->
                    <Border Grid.Row="1" x:Name="ProcessesPage" Visibility="Collapsed" Style="{StaticResource Card}" Padding="0">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <Border Grid.Row="0" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                                <Grid>
                                    <TextBox x:Name="ProcessSearchBox" Style="{StaticResource ModernTextBox}" TextChanged="ProcessSearch_TextChanged"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                                        <Button Content="{DynamicResource Loc.Refresh}" Style="{StaticResource GhostButton}" Click="ProcessRefresh_Click"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <ListBox Grid.Row="1" x:Name="ProcessListBox" Style="{StaticResource ModernListBox}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <EventSetter Event="PreviewMouseRightButtonDown" Handler="ProcessListItem_RightButtonDown"/>
                                        <EventSetter Event="ContextMenuOpening" Handler="ProcessListItem_ContextMenuOpening"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" CornerRadius="8" Margin="0,0,12,0" Background="{StaticResource PrimaryBgBrush}">
                                                <TextBlock Text="⚙" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding Path}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <Border Grid.Column="2" CornerRadius="10" Padding="8,2" VerticalAlignment="Center" Margin="0,0,12,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource SurfacePressedBrush}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasMatchedRule}" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource PrimaryBgBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding RuleLevelText}" FontSize="10" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding HasMatchedRule}" Value="True">
                                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                                <TextBlock FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}">
                                                    <Run Text="{DynamicResource Loc.Pid}"/><Run Text=" "/><Run Text="{Binding Pid}"/>
                                                </TextBlock>
                                                <TextBlock Text="{Binding AffinityShort}" FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <!-- ═══ Rules ═══ -->
                    <ScrollViewer Grid.Row="1" x:Name="RulesPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <ItemsControl x:Name="RulesList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" IsChecked="{Binding Enabled, Mode=TwoWay}" Tag="{Binding Id}" Style="{StaticResource ModernCheckBox}" VerticalAlignment="Center" Margin="0,0,12,0" Checked="RuleEnabled_Changed" Unchecked="RuleEnabled_Changed"/>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                        <Border Style="{StaticResource Tag}" Margin="0,0,6,0">
                                                            <TextBlock Text="{Binding ModeDisplay}" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"/>
                                                        </Border>
                                                        <Border CornerRadius="12" Padding="10,3">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="{StaticResource PrimaryBgBrush}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding LevelText}" Value="job-locked"><Setter Property="Background" Value="#FEE2E2"/></DataTrigger>
                                                                        <DataTrigger Binding="{Binding LevelText}" Value="job-enforced"><Setter Property="Background" Value="#FEE2E2"/></DataTrigger>
                                                                        <DataTrigger Binding="{Binding LevelText}" Value="hard-affinity"><Setter Property="Background" Value="#FEF3C7"/></DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <TextBlock Text="{Binding LevelText}" FontSize="10" FontWeight="SemiBold">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="#6366F1"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding LevelText}" Value="job-locked"><Setter Property="Foreground" Value="#991B1B"/></DataTrigger>
                                                                            <DataTrigger Binding="{Binding LevelText}" Value="job-enforced"><Setter Property="Foreground" Value="#DC2626"/></DataTrigger>
                                                                            <DataTrigger Binding="{Binding LevelText}" Value="hard-affinity"><Setter Property="Foreground" Value="#C2410C"/></DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding MatchDisplay}" FontSize="11" Margin="0,4,0,0" Foreground="{StaticResource TextMutedBrush}"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Button Content="✎" Style="{StaticResource IconButton}" ToolTip="{DynamicResource Loc.EditRule}" Click="EditRule_Click" Tag="{Binding Id}"/>
                                                    <Button Content="✕" Style="{StaticResource IconButton}" ToolTip="{DynamicResource Loc.DeleteRule}" Click="DeleteRule_Click" Tag="{Binding Id}" Foreground="{StaticResource DangerBrush}"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Border Style="{StaticResource Card}" Padding="40" HorizontalAlignment="Center" Margin="0,20,0,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="📏" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="{DynamicResource Loc.NoRules}" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock Text="{DynamicResource Loc.CreateFirstRule}" Style="{StaticResource BodyText}" HorizontalAlignment="Center" Margin="0,4,0,12"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- ═══ Settings ═══ -->
                    <ScrollViewer Grid.Row="1" x:Name="SettingsPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Border Style="{StaticResource Card}" Padding="20" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Loc.Monitoring}" Style="{StaticResource Heading2}" Margin="0,0,0,8"/>
                                    <CheckBox x:Name="ChkWmiMonitor" IsChecked="True" Content="{DynamicResource Loc.EnableMonitor}" Style="{StaticResource ModernCheckBox}" Margin="0,4" Checked="Settings_Changed" Unchecked="Settings_Changed"/>
                                    <CheckBox x:Name="ChkMinimizeTray" IsChecked="True" Content="{DynamicResource Loc.MinimizeToTray}" Style="{StaticResource ModernCheckBox}" Margin="0,4"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource Card}" Padding="20" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Loc.Language}" Style="{StaticResource BodyText}" VerticalAlignment="Center"/>
                                    <ComboBox x:Name="LanguageSelector" Grid.Column="1" SelectedIndex="0" SelectionChanged="LanguageSelector_SelectionChanged">
                                        <ComboBoxItem Content="English"/>
                                        <ComboBoxItem Content="简体中文"/>
                                    </ComboBox>
                                </Grid>
                            </Border>
                            <Border Style="{StaticResource Card}" Padding="20">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Loc.About}" Style="{StaticResource Heading2}" Margin="0,0,0,8"/>
                                    <TextBlock Style="{StaticResource BodyText}"><Run Text="{DynamicResource Loc.ProductVersion}"/></TextBlock>
                                    <TextBlock Style="{StaticResource BodyText}" Margin="0,2,0,0"><Run Text="{DynamicResource Loc.ProductDescription}"/></TextBlock>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" CornerRadius="0,0,10,10" Padding="16,6">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Border Width="8" Height="8" CornerRadius="4" x:Name="StatusDot" Background="{StaticResource SuccessBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtStatus" Text="{DynamicResource Loc.Ready}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="v2.0.0" FontSize="11" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
    </Border>
    </Border>
</Window>
