<UserControl x:Class="CpuAffinityManager.App.Controls.CoreTypeLegend"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="200">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="8" CornerRadius="4">
        <StackPanel>
            <TextBlock Text="{DynamicResource Loc.CoreLegend}" FontWeight="Bold" Margin="0,0,0,6"/>
            <StackPanel Orientation="Horizontal" Margin="0,2">
                <Rectangle Width="16" Height="16" Fill="#E74856" RadiusX="2" RadiusY="2" Margin="0,0,6,0"/>
                <TextBlock Text="{DynamicResource Loc.PCoreLegend}" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,2">
                <Rectangle Width="16" Height="16" Fill="#0099BC" RadiusX="2" RadiusY="2" Margin="0,0,6,0"/>
                <TextBlock Text="{DynamicResource Loc.ECoreLegend}" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,2">
                <Rectangle Width="16" Height="16" Fill="#FFB900" RadiusX="2" RadiusY="2" Margin="0,0,6,0"/>
                <TextBlock Text="{DynamicResource Loc.SmtThread}" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
