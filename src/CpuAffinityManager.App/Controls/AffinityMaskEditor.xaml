<UserControl x:Class="CpuAffinityManager.App.Controls.AffinityMaskEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="64" d:DesignWidth="400">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="8">
        <StackPanel>
            <TextBlock Text="{DynamicResource Loc.MaskEditor}" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock Text="{DynamicResource Loc.MaskEditorTodo}" Foreground="Gray" FontStyle="Italic"/>
            <!-- Each checkbox represents one logical processor bit -->
        </StackPanel>
    </Border>
</UserControl>
